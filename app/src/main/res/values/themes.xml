<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/BlueGrotto</item>
        <item name="colorPrimaryVariant">@color/BlueGrotto</item>
        <item name="android:statusBarColor">@color/BlueGrotto</item>
    </style>

    <style name="Theme.MyNote" parent="Base.Theme.MyNote" />
</resources>